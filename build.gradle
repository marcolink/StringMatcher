plugins {
    id 'net.wooga.paket' version '0.10.1'
}

publishing {
    repositories {
        nuget {
            name "nuget"
            url "https://www.nuget.org/api/v2/package"
        }
    }
}